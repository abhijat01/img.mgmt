import torch.nn as nn
import torch.nn.functional as F

class DD(nn.Module):
    def __init__(self):
        super(DD, self).__init__()
        #TODO, need to add

    def backward(self, x):
        #TODO run through layers
        return x


dd = DD()